import React, { SVGProps } from "react";
import { MdOutlineLogout } from "react-icons/md";

const LogoutIcon = (props: SVGProps<SVGSVGElement>) => <MdOutlineLogout {...props} />;

export default LogoutIcon;
