import type { SVGProps } from "react";
import React from "react";
import { RiLoginCircleLine } from "react-icons/ri";

const LoginIcon = (props: SVGProps<SVGSVGElement>) => <RiLoginCircleLine {...props} />;

export default LoginIcon;
