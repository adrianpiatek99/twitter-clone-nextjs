import type { SVGProps } from "react";
import React from "react";
import { AiFillGithub } from "react-icons/ai";

const GithubIcon = (props: SVGProps<SVGSVGElement>) => <AiFillGithub {...props} />;

export default GithubIcon;
