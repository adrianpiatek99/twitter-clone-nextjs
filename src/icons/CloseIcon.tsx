import type { SVGProps } from "react";
import React from "react";
import { CgClose } from "react-icons/cg";

const CloseIcon = (props: SVGProps<SVGSVGElement>) => <CgClose {...props} />;

export default CloseIcon;
