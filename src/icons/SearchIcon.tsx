import React, { SVGProps } from "react";
import { RiSearchFill } from "react-icons/ri";

const SearchIcon = (props: SVGProps<SVGSVGElement>) => <RiSearchFill {...props} />;

export default SearchIcon;
