import type { SVGProps } from "react";
import React from "react";
import { BiArrowBack } from "react-icons/bi";

const BackIcon = (props: SVGProps<SVGSVGElement>) => <BiArrowBack {...props} />;

export default BackIcon;
