import type { SVGProps } from "react";
import React from "react";
import { BsPlus } from "react-icons/bs";

const PlusIcon = (props: SVGProps<SVGSVGElement>) => <BsPlus {...props} />;

export default PlusIcon;
